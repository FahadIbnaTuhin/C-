// This file doesn't need the same file name with compare.h. You can name anything you want. 
// But it's easy to understand

// You can use multiple file to write your code. Compiler automatically
// find them if they are global. Used compare1.cpp here 

int max(int a, int b) {
    if (a > b) {
        return a;
    } else {
        return b;
    }
}